> **⚠️ AI AGENT: Read `AGENTS.md` at the repo root first. This initiative is subordinate to it.**

# OpenMuara Web UI SPA — Handoff

## Current Status

- Initiative created on `feat/web-ui-spa`.
- Prompt 01 completed: Vite + Preact selected.
- No product code changes yet.

## Open Questions

1. Should the escape/pay pages be migrated to the SPA, or kept as server-rendered HTML?
2. Is TypeScript desired for the new dashboard?
3. Site directory name (`web/dashboard/`, `ui/`, or `dashboard/`)?

## Next Actions

- [ ] Scaffold the Vite + Preact build pipeline in prompt 02.
- [ ] Decide whether escape/pay pages move into the SPA or stay server-rendered.
