# OpenMuara ToyyibPay — Prerequisites

Before starting this initiative, confirm:

- [ ] You are on the `dev` branch.
- [ ] `go test ./...` passes on `dev`.
- [ ] You understand the existing provider registry in `internal/provider/`.
- [ ] You have read `REFERENCES.md` for ToyyibPay documentation.
- [ ] You understand the existing webhook dispatcher in `internal/webhook/`.
